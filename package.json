{
  "name": "canvas-test",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "build": "yarn run electron-rebuild && yarn run electron-rebuild -f -w -o better-sqlite3 && yarn run electron-rebuild -f -w -o skia-canvas"
  },
  "dependencies": {
    "better-sqlite3": "^7.6.2",
    "skia-canvas": "^1.0.1"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.2.9",
    "electron": "^19.1.3",
    "prebuild-install": "^7.1.1"
  },
  "resolutions": {
    "nan": "2.14.0"
  }
}
